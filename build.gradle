plugins{
    id('java')
}

version = "1.0.0"

allprojects {
    sourceCompatibility = 1.8

    repositories {
        mavenCentral()
        jcenter()
    }

    dependencies {
        testCompile group: 'junit', name: 'junit', version: '4.12'
    }

    wrapper{
        gradleVersion = '5.0'
    }

    group = "com.honesmind.jtribe-challenge"
    
}